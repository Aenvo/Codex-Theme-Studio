# Codex Theme Studio 项目协作规则

## 1. 适用范围与优先级

本文件适用于 `Codex Theme Studio` 项目根目录及其全部子目录。

执行优先级：

1. 系统、开发者与工具安全规则。
2. 用户在当前对话中的明确指令。
3. 当前提交的编号任务文档。
4. 更深目录中的 `AGENTS.override.md` 或 `AGENTS.md`。
5. 本文件。
6. 项目其他背景文档与代码推断。

当前编号任务与本文件冲突时，以用户当前任务为准，并在最终回复中说明冲突和处理方式。不得把推断写成已确认事实；不确定信息标记为 `To be confirmed`。

## 2. 项目身份

- 项目正式工作名：**Codex Theme Studio**。
- 前期任务文档使用过 **Codex Theme Manager**；二者在本项目中指同一个产品，不要创建两个解决方案或重复模块。
- 这是非 OpenAI 官方产品。不得使用会造成官方背书误解的名称、图标、文案或发布描述。
- 第一版目标是 Windows 本地桌面应用，不是 Codex 插件，也不是在线主题商店。

## 3. 产品目标

制作一个面向普通用户的轻量 Windows 主题管理器，用于：

- 创建、编辑、复制、导入、导出和管理多个 Codex 桌面端主题。
- 上传本地背景图片并调整色值、焦点、安全区、遮罩、透明度和模糊度。
- 在首页和普通任务页采用不同背景强度。
- 临时显示主题。
- 启用或停用持久化。
- 在主题间安全切换。
- 一键还原 Codex 官方外观。
- 允许用户选择和迁移主题数据存储位置。
- 以便携 ZIP 为主要发布形式，解压后即可运行。

## 4. 已确认的第一版技术基线

除非用户或已批准的 ADR 明确变更，默认采用：

- 目标平台：Windows 10/11 x64。
- GUI：.NET 8 + WPF。
- 发布方式：Windows x64 self-contained 便携目录和 ZIP。
- 第一版不强求真正单文件 EXE；允许主 EXE 搭配受管 Runtime 目录。
- 用户无需预装 .NET、Node.js、npm 或 npx。
- 第一版允许随包捆绑固定版本 Node Runtime，用于经过审计的注入助手。
- SQLite 保存主题索引和元数据。
- 图片、缩略图、主题 JSON 和导出包存放在文件系统。
- 本地优先；已有主题在离线状态下仍应可以应用。
- 正常功能不要求管理员权限。
- GUI 可退出；持久化由独立轻量 Agent 完成。
- 调试接口只短时开放，注入完成后关闭。

技术基线变更必须：

1. 说明触发变更的证据。
2. 评估对已有任务、数据格式、发布包和恢复路径的影响。
3. 获得用户确认或遵循当前任务的明确授权。
4. 更新已有 ADR；不要只在代码里悄悄改变架构。

## 5. 十二阶段执行协议

用户会依次在十二个独立对话中提交编号任务文档。每个对话只执行当前文档规定的任务。

任务顺序：

1. 项目基线与技术决策。
2. 解决方案脚手架与模块契约。
3. 主题格式、数据库与存储抽象。
4. 图片安全处理与预览资源管线。
5. Codex 发现、身份校验与短时 Inspector 通道。
6. 渲染器注入、窗口隔离与主题生命周期。
7. 临时应用、状态查询与完整还原。
8. 持久化 Agent 与当前主题快照。
9. WPF 桌面壳与多主题资料库界面。
10. 主题编辑、模拟预览、导入导出与存储迁移。
11. 集成、安全、兼容性与真实场景测试。
12. 便携打包、发布文档与最终验收。

执行规则：

- 开始前读取本文件、当前任务文档、已有 ADR、相关代码和测试。
- 检查 Git 状态并保留用户或前序任务的未提交修改。
- 只完成当前任务范围，不提前实现下一编号任务。
- 当前任务依赖的前序交付缺失时，先进行最小审计；不能安全补齐时，报告具体阻塞，不擅自扩大范围。
- 不因后续会需要某功能，就在当前阶段引入未使用的框架、服务或抽象。
- 完成当前任务后执行任务文档要求的验证并停止。
- 不自动开始下一任务，不创建新对话，不公开发布，不上传构建产物。
- 最终回复必须给出结果、变更文件、验证、未完成事项或风险。

## 6. 参考项目与知识产权边界

可以研究以下公开项目的架构和安全思想：

- `fanbidog/okkmax-web`
- `fanbidog/okkskin`
- `Fei-Away/Codex-Dream-Skin`

允许参考的内容包括：

- Electron/Chromium 调试接口的工作方式。
- 回环地址、Browser ID、PID、Appx 身份和目标窗口校验思想。
- 主题数据建模、图片焦点、安全区和任务页弱化思想。
- 辅助窗口隔离、幂等注入、Blob URL 清理和测试场景。

限制：

- 不直接复制许可范围不明确的 Windows 源码、CSS、脚本或素材。
- 复用具体代码前必须确认许可证覆盖范围并保留许可证与版权声明。
- 不打包参考仓库的人物、IP、品牌或效果图素材。
- 测试和默认主题使用自制或许可明确的抽象素材。
- 独立实现核心模块，保留可追溯的第三方 Notices。

## 7. 架构边界

推荐解决方案模块：

```text
src/
├─ CodexThemeStudio.Desktop/
├─ CodexThemeStudio.ThemeCore/
├─ CodexThemeStudio.Storage/
├─ CodexThemeStudio.CodexAdapter/
├─ CodexThemeStudio.Agent/
└─ CodexThemeStudio.Contracts/

runtime/
└─ injector/

tests/
├─ ThemeCore.Tests/
├─ Storage.Tests/
├─ CodexAdapter.Tests/
└─ Integration.Tests/
```

若任务 1 的 ADR选择了不同命名，以 ADR 为准，但保持以下依赖原则：

- ThemeCore 不依赖 WPF、SQLite、Node 或 Codex。
- Storage 实现仓储和数据位置接口。
- CodexAdapter 不依赖 Desktop。
- Agent 不依赖 WPF。
- Desktop 不直接执行拼接的 Shell、PowerShell 或 Node 命令。
- 跨进程请求使用结构化 DTO 和结构化错误。
- 主题数据不能拼入命令行。
- 系统操作必须支持超时、取消和明确失败状态。

## 8. 主题与数据规则

主题必须是版本化、声明式数据。第一版只允许：

- 背景图片。
- 经过白名单验证的颜色。
- focusX/focusY。
- safeArea。
- cover/contain 等有限布局值。
- 首页与任务页的透明度、遮罩、模糊和显示模式。
- auto/light/dark。

主题包不得包含或执行：

- 任意 JavaScript。
- 任意命令或可执行文件。
- 任意 HTML。
- 任意远端 CSS。
- 生命周期 Hook。
- 用户凭证、绝对路径、日志或身份数据。

数据原则：

- 主题 ID 使用 UUID，不使用显示名称作为目录名。
- SQLite 保存索引，不保存大型图片 Blob。
- 主题文件路径相对于可信 DataRoot。
- 阻止 `..`、绝对路径、符号链接、 Junction 和 Zip Slip 逃逸。
- JSON、状态和定位文件使用临时文件加原子替换。
- 数据库写入使用事务。
- 未知高版本 Schema 不得被旧程序覆盖。
- 不自动删除数据库外的孤立文件；先报告或提供可恢复处理。

## 9. 可更换存储位置

应用允许用户选择实际数据根目录。固定位置只保留必要的小型定位和运行数据。

迁移必须遵循：

1. 暂停写操作和 Agent 主题切换。
2. 检查目标权限、空间与文件系统状态。
3. 复制数据。
4. 校验文件数量、大小和 SHA-256。
5. 打开新数据库并执行完整性检查。
6. 原子更新数据根定位。
7. 更新 Agent 和当前主题快照。
8. 验证应用和持久化。
9. 保留旧目录作为恢复点。

不得：

- 迁移未验证成功就切换 DataRoot。
- 迁移失败后删除旧数据。
- 外置磁盘离线时静默创建一个新的默认空库。
- 未经用户确认永久删除旧目录。

## 10. 图片安全规则

- 只接受项目明确支持的 PNG、JPEG、WebP。
- 按内容识别格式，不只信任扩展名。
- 禁止 SVG 和带代码能力的格式。
- 执行大小、边长和总像素限制。
- 完整解码后重新编码为受管副本。
- 默认删除 EXIF、GPS 和不必要元数据。
- 生成独立缩略图，不在列表中反复解码完整原图。
- 导入失败或取消后只清理本次暂存文件。
- 不上传用户图片做云端主体识别或调色。

第一版默认安全上限为 16 MB、单边 16384 像素、总计 5000 万像素；如 ADR 调整，测试和文档必须同步。

## 11. Codex 注入安全边界

主题生效发生在本机 Electron 运行时。必须：

- 只接受当前用户已注册的官方 Microsoft Store Codex 包。
- 动态发现当前 Appx 路径，不长期信任版本目录。
- 校验包身份、进程路径、PID 和进程创建时间。
- 排除 Renderer/GPU 等 `--type=` 子进程。
- 只连接回环地址。
- 验证调试端口、WebSocket 路径、Browser ID、Page Target ID 和 `app://` URL。
- 防止端口复用、PID 复用和竞态条件。
- 使用短超时、有限响应和结构化错误。
- 注入或探测结束后关闭 Inspector。
- 未识别的 Codex 版本或窗口默认 fail-closed。

禁止：

- 修改、替换或取得 `WindowsApps` 所有权。
- 修改 `app.asar`、官方 EXE、Appx 或数字签名。
- 为完成主题功能绕过系统安全控制。
- 长期开放 CDP/Inspector。
- 控制身份无法确认的 Electron 进程。
- 读取或记录用户对话 DOM、认证数据或令牌。

## 12. 窗口隔离与渲染规则

历史已确认问题：向所有 `app://` 窗口注入会覆盖宠物背景。

窗口识别至少采用：

1. URL/路由白名单与排除规则。
2. `initialRoute=/avatar-overlay` 明确排除。
3. 完整 Codex 主界面的 DOM 特征探测。
4. 无法识别的辅助窗口保持原样。

主题运行时必须：

- 保持宠物和其他辅助透明窗口无背景、无主题 Class、无 Style。
- 保持原生侧栏、项目选择器、菜单、弹窗、Composer 和滚动可交互。
- 装饰层使用 `pointer-events: none`。
- 重复应用幂等。
- 切换主题时清理旧 Style、Class、DOM 和 CSS 变量。
- 撤销旧 Blob URL，避免内存积累。
- Restore 后停止未来自动重注。
- 首页允许更突出背景，普通任务页默认降低干扰。

## 13. 临时显示、持久化与还原语义

### 临时显示

- 只影响当前 Codex 实例。
- 不创建启动项。
- 不启动持久化 Agent。
- Codex 完全退出后效果消失。

### 持久化

- 使用验证后的当前主题快照。
- GUI 可以退出。
- Agent 发现新的可信 Codex PID 后短时注入。
- 同一 PID 不应频繁重复注入。
- 持久化必须由用户明确启用。

### 还原 Codex 外观

- 清除运行时主题。
- 停止当前进程内未来重注。
- 关闭 Inspector。
- 保留主题库和应用设置。

### 停用持久化

- 移除当前用户启动项。
- 停止 Agent。
- 撤销当前运行时主题。
- 保留主题库。

不得把“还原外观”“停用持久化”“删除主题库”“重置应用设置”合并成一个不可区分的危险操作。

## 14. 隐私与联网

- 默认不联网。
- 本地主题的编辑、预览、切换和持久化不依赖外部服务器。
- 不上传背景图片、主题数据、日志、Codex 页面或用户对话。
- 不读取或修改 API Key、Base URL、模型供应商、MCP、身份或权限配置。
- 如后续加入远端主题源，必须单独设计下载白名单、签名、SRI、防回滚和隐私说明，并取得用户授权。
- 日志使用最少必要原则，支持大小上限和用户查看。

## 15. UI 规则

- UI 任务开始前读取 `design.md`；缺失时只报告，不自动创建。
- 优先复用现有控件、样式资源和 Design Tokens。
- 主题列表需支持缩略图、搜索、排序、收藏和标签。
- 当前选中、当前临时主题和当前持久主题必须视觉上可区分。
- 长操作异步执行，不冻结窗口。
- Apply、Switch、Restore 和迁移期间禁用冲突操作。
- 错误面向用户说明原因和下一步，不直接显示原始堆栈。
- 支持键盘导航、Focus、高 DPI 和减少动态效果设置。
- 模拟预览必须标明不是实际 Codex 渲染验证。
- 不把真实对话或带完整 UI 的截图作为背景素材。

## 16. 代码与变更原则

- 修改前做当前任务所需的轻量审计。
- 保持改动小、聚焦、可回滚。
- 复用现有接口和工具，不重复创建等价模块。
- 不重构无关代码。
- 不新增依赖，除非当前任务确实需要并已检查许可证和发布影响。
- 不硬编码当前用户绝对路径。
- 不使用字符串拼接构造 Shell 命令。
- 不吞掉安全相关异常；返回结构化错误并记录脱敏诊断。
- 不执行 `git reset --hard`、破坏性清理或永久删除。
- 不覆盖未跟踪文件、截图、日志、用户主题或前序任务产物。
- 不自动提交、推送、创建 PR、Release 或上传构建产物，除非用户明确要求。

## 17. 验证要求

每项变更按风险执行最小可靠验证：

- syntax
- build
- unit tests
- integration tests
- format/lint（项目存在时）
- file format and path checks
- git diff/status
- GUI screenshot/interaction
- real Codex validation（任务要求且用户允许时）

结论必须与证据对应：

- 构建成功不等于真实注入成功。
- 进程存在不等于主题可见。
- 注入标记存在不等于布局和交互正确。
- 配置正确不等于持久化实际执行。

真实 Codex 验证至少关注：

- 首页。
- 普通任务页。
- 侧栏。
- 项目选择器。
- Composer。
- 菜单、弹窗和滚动。
- 宠物窗口。
- 路由变化和渲染器刷新。
- 多次主题切换。
- Restore/reapply。
- 启用和停用持久化。

测试时不要发送真实消息；截图和日志需隐藏账号、私人任务名和对话内容。

## 18. 发布与恢复

第一版发布包必须：

- 无需预装 .NET 或 Node。
- 不要求管理员权限。
- 不包含开发机绝对路径。
- 不包含私人图片、日志、数据库、凭证或测试残留。
- 附带第三方许可证和 Notices。
- 记录 ZIP 和主要 EXE 的 SHA-256。
- 如未签名，如实说明。
- 在路径含空格和中文时验证。

不得未经授权：

- 创建公开 GitHub Release。
- 上传安装包或主题。
- 购买或申请代码签名证书。
- 启用自动更新。
- 永久删除旧数据或恢复副本。

## 19. 文档规则

- 面向用户的文档默认使用中文。
- 代码标识符、API、文件名和命令保持项目既有语言。
- README、用户指南必须准确说明临时显示、持久化、还原配置、数据位置和完整清理。
- 项目文档使用相对路径，不写入开发机个人绝对路径。
- `AGENTS.md` 只记录长期稳定规则，不记录某次临时测试结果。
- `design.md` 只记录可复用视觉规则，不记录单页临时微调。
- 缺失长期文档时只报告；除非当前任务明确要求，不自动创建。

## 20. 每个任务的最终回复

默认使用以下结构：

1. 结果。
2. 变更文件。
3. 验证。
4. 未完成事项、风险或下一任务输入。

文件使用可点击的绝对 Markdown 链接。明确说明未执行的验证及原因。

当前编号任务完成后必须停止，并提醒用户下一份应提交的任务文档；不要继续实现下一阶段。
