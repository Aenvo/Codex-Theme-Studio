# 任务 11 已知问题与未覆盖风险

## 明确跳过

1. 本次未重新执行真实持久化 enable/disable。现有稳定 Agent 目录只包含 Node
   `v24.16.0`，不满足 ADR 固定的 `v24.18.0`；执行还会写入当前用户 Run 项。
   高风险审计结论为 `Pause`，等待任务 12 生成并核验正式 Runtime。
2. 本次未真实重启 Codex，也未真实迁移用户 DataRoot。相应事务、离线和恢复路径
   已由自动化覆盖，但不能替代现场证据。
3. 本次没有在真实首页执行主题验证；真实任务页和刷新恢复已验证，首页由 Renderer
   自动化覆盖。

## 性能与发布边界

- Agent 空闲 CPU/内存未测：本次没有安全启动不符合固定 Runtime 基线的 Agent。
- 最终便携目录和 ZIP 大小未测：任务 11 不开始任务 12 的打包。
- 当前 Release 构建目录包含开发运行时和重复依赖，只能作为工程体积参考，不能当作
  最终发布包大小。
- 100/500 主题结果来自 ViewModel 夹具，不包含真实磁盘缩略图解码和低速外置盘。

## 隐私与截图

- 真实 Codex 的现场画面包含本地任务内容，只用于当次目视核对，没有保存。
- 仓库只保存不含私人内容的 Theme Studio 冷启动截图。
- 没有发送真实消息，没有读取或记录页面正文、账号、Token、认证数据或完整 URL。

## 历史证据边界

本机现有脱敏 Agent 日志、状态和 disabled 配置显示任务 8 曾在当前 Codex 进程上
运行持久化循环，当前 Run 值为空、Agent 未运行、配置 `enabled=false`。这只能作为
历史恢复证据，不等于任务 11 在固定 Node `v24.18.0` 上完成复验。

