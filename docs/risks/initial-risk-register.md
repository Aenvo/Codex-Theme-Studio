# 初始风险清单

- 日期：2026-07-20
- 范围：任务 1 技术基线；状态将在对应编号任务中更新
- 最近更新：2026-07-20（任务 6）

| ID | 风险 | 影响 | 当前控制与后续验证 | 责任阶段 | 状态 |
| --- | --- | --- | --- | --- | --- |
| R-01 | 当前机器没有全局 .NET SDK | 后续脚手架无法直接构建 | 已将经任务 1 校验的 SDK 8.0.423 部署到 `%LOCALAPPDATA%\CodexThemeStudio\devtools`；`global.json` 固定版本，`build.ps1` 自动发现且不修改 PATH | 任务 2 | Mitigated |
| R-02 | .NET 8 于 2026-11-10 结束支持 | 发布后可能缺少安全修复 | 发布前使用最新 8.0 补丁；若时间线跨过 EOL，以新 ADR 评估迁移到受支持的 LTS | 任务 11–12 | Open |
| R-03 | self-contained .NET 与 Node Runtime 使便携包较大 | 下载、解压和冷启动体验下降 | 采用便携目录而非强制单文件；任务 12 测量 ZIP/解压体积和启动时间，不以裁剪破坏 WPF 或反射行为 | 任务 12 | Open |
| R-04 | 捆绑 Node Runtime、SQLite 与 Skia 原生库形成供应链面 | 漏洞、许可证遗漏、包体增大或文件被替换 | 已固定 SDK、测试依赖、`Microsoft.Data.Sqlite` 8.0.29、`SQLitePCLRaw.bundle_e_sqlite3` 3.0.3、MIT 许可的 `SkiaSharp` 4.150.1 和 Injector 协议/Node 版本，并完成零已知 NuGet 漏洞复扫。原生二进制哈希、Notices、包体和发布前扫描仍留给任务 11–12 | 任务 11–12 | Mitigating |
| R-05 | 错误连接非官方或已被替换的 Electron 进程 | 控制错误进程或泄露本地页面信息 | 任务 5 已固定校验 Store PackageFamilyName、PublisherId、签名来源、包内路径、主进程命令行、PID 与创建时间，并在连接前后重取快照；Browser/Page Target 与 `app://` 路由采用严格结构校验，竞态时 fail-closed | 任务 5 | Mitigated |
| R-06 | Inspector 端口长期开放或端口复用 | 本地攻击面扩大 | 任务 5 已完成回环端点、监听 PID、超时与响应上限控制；任务 6 的 prepare/probe/apply/ensure/status/cleanup 真实流程均在结束后确认 9229 无监听，且 Inspector 关闭后只保留当前可信进程内的有 Guard 生命周期钩子。任务 7、11 继续复验应用层还原与异常路径 | 任务 5–7、11 | Mitigating |
| R-07 | 主题误注入 `avatar-overlay` 或其他辅助窗口 | 宠物透明窗口被覆盖、原生 UI 异常 | 任务 6 已实现 `app://`/辅助路由预筛与版本化 Shell、侧栏、主内容、Composer 四类 DOM 特征复核；VM 和真实截图均确认 1 个主窗口应用、1 个 `avatar-overlay` 窗口保持透明，未知/不完整窗口不注入 | 任务 6、11 | Mitigated |
| R-08 | 主题包或主题字段绕过声明式白名单 | 任意脚本、命令、远端 CSS 或路径逃逸 | 任务 3 已实现 Schema v1 严格字段/枚举/数值/颜色/路径白名单，当前版本未知字段拒绝、未知高版本只读报告且禁止覆盖；主题数据不进入命令行仍由任务 6、10 验证 | 任务 3、6、10 | Mitigating |
| R-09 | 图片解码炸弹、恶意元数据或伪装格式 | 内存耗尽、隐私泄露或解析器风险 | 任务 4 已按文件头和解码器双重识别 PNG/JPEG/WebP，在完整解码前限制 16 MiB、16384 单边和 5000 万像素，拒绝动画/截断内容，应用 EXIF 方向后以像素重新编码 WebP，不复制 EXIF/GPS/ICC；任务 11 继续模糊测试和真实大图压力验证 | 任务 4、11 | Mitigating |
| R-10 | DataRoot 中的 `..`、符号链接、Junction 或 Zip Slip | 读写逃出可信目录并覆盖用户文件 | 任务 3 已规范化相对路径并拒绝绝对路径、`..`、保留名和现有重解析点；任务 4 的图片处理使用 DataRoot 内独立暂存目录；任务 10 的 `.cttheme` 导入只接受根目录白名单条目并验证大小、数量和 SHA-256。更强竞态加固留给任务 11 | 任务 3、4、10、11 | Mitigating |
| R-11 | 存储迁移在复制或切换中失败 | 主题库损坏、数据丢失或出现空库 | 任务 10 已实现忙碌状态与迁移锁、空间/权限检查、暂存复制、文件数量/大小/SHA-256、SQLite 完整性检查、原子 bootstrap 切换和源目录保留；跨进程中断与真实外置磁盘场景留给任务 11 | 任务 10–11 | Mitigating |
| R-12 | 临时显示、持久化、还原和停用语义混淆 | 用户以为已恢复，Agent 却继续注入，或误删主题库 | 使用独立命令和结构化状态；Restore 停止未来重注；停用持久化保留主题库 | 任务 7–8、11 | Open |
| R-13 | GUI、Agent 与 Codex 同时切换主题 | 竞态、重复注入、状态快照不一致 | 原子快照、PID 去重、超时与取消、冲突操作禁用、崩溃恢复测试 | 任务 7–9、11 | Open |
| R-14 | Windows 路径含空格、中文或外置磁盘离线 | 启动、SQLite、Node 子进程或迁移失败 | 任务 2 已在含空格的项目路径下通过构建与自检；任务 3 已覆盖中文与 Emoji 主题名；任务 10 已在 DataRoot 离线时显示明确状态、禁用迁移并拒绝创建替代空库。真实外置磁盘断连与恢复场景留给任务 11–12 | 任务 10–12 | Mitigating |
| R-15 | 项目当前不是 Git 仓库 | 无法用 `git status` 隔离任务变更或建立提交恢复点 | 已初始化仅本地 `main` Git 仓库并添加构建产物忽略规则；没有 remote、commit 或 GitHub 关联 | 任务 2 | Mitigated |
| R-16 | `design.md` 和 README 尚不存在 | 后续 UI 约束和用户说明可能缺失 | 当前只记录缺口；UI 阶段开始前确认设计约束，README 在用户授权的对应阶段创建 | 任务 9、12 | Open |
| R-17 | 产品名称或视觉表达造成 OpenAI 官方背书误解 | 商标、分发和用户信任风险 | 最小 WPF 窗口已显示“非 OpenAI 官方工具”，且未使用官方图标；任务 9 和 12 继续验证完整界面及发布文案 | 任务 9、12 | Mitigating |
| R-18 | 参考项目许可或素材边界不清晰 | 版权与发布风险 | 只参考公开技术思想；具体代码复用前核对许可证并保留声明；不打包人物/IP 素材 | 全阶段 | Open |
