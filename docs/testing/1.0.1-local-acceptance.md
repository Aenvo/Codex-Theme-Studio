# 1.0.1 本地验收记录

## 范围

本记录对应 Windows x64 self-contained 本地维护版本 `1.0.1`，重点复验图片缓存并发提交、固定 Runtime、便携打包和清理后的安全状态。它不代表公开发布、代码签名、干净 Windows/VM、有效病毒扫描、Codex 完全重启或真实外置磁盘迁移已经通过。

## 自动化与并发

- Release 构建：0 警告、0 错误。
- .NET：123 项通过；2 项需要显式现场授权的持久化测试按设计跳过。
- 图片缓存并发测试：每轮 16 个不同主题并发处理同一图片，连续 50 轮全部通过。
- 运行时图片提交失败后，共享预览和缩略图缓存保留并可由后续操作复用。
- `dotnet format --verify-no-changes`、Agent self-test 和 Injector self-test：通过。
- 固定 Node `v24.18.0` 的 Injector 测试：23 项通过。
- NuGet 直接及传递依赖漏洞扫描：未报告已知漏洞包。

## 便携产物

| 项目 | 结果 |
| --- | --- |
| 版本 | `1.0.1` |
| RID | `win-x64` |
| 目录文件数 | 693 |
| 目录总大小 | 365,829,606 B |
| ZIP 大小 | 151,380,760 B |
| ZIP SHA-256 | `2d90c38ceb0279c194e4884c6a68420613278663abc6efe85db169f8e820ab61` |
| 主 EXE SHA-256 | `1563a51b174fe152966511fbdf58a1a21874dc0285a8af429a0349e053973b81` |
| Agent EXE SHA-256 | `bcb818cb650766b34f1482185a63e8c4bba1179dc07b17fc275360910c4a2b77` |
| 主 EXE / Agent 签名 | `NotSigned` / `NotSigned` |

重新计算的文件数、字节数和三项哈希均与 `release-manifest.json`、`SHA256SUMS.txt` 一致。未发现 PDB、数据库、`.cttheme`、测试、fixture、截图或日志目录；文本扫描未发现开发机用户名或个人路径。便携 README 的 4 个本地链接均可解析，打包成功后本次唯一工作目录已回收。

## 启动与最终状态

- ZIP 解压到 `1.0.1 最终 含中文 空格` 验证目录后，`CodexThemeManager.exe` 创建了标题为 `Codex Theme Studio` 的真实主窗口。
- 随包 Node 报告 `v24.18.0`；便携 Agent 和 Injector self-test 均返回 `ok`。
- 验收没有执行持久化 enable/disable，也没有创建启动项。
- 最终状态：当前用户 Run 值不存在、Agent 进程数为 0、持久化配置为 `enabled=false`、Inspector 端口 `9229` 监听数为 0。

## 未覆盖

- 无开发 Runtime 的干净 Windows 用户或虚拟机。
- 承载当前会话的 Codex 完全退出和重启。
- 有效的本机防病毒扫描。
- 当前用户真实 DataRoot 的物理外置磁盘迁移与断连。
