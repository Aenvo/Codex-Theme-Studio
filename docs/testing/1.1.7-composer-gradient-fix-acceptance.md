# 1.1.7 Composer 渐变暗区修复验收

## 修复范围

- 保持 Doro Composer 背景为 `#1A151F`，并保留主题 Border 色的一像素轮廓。
- 仅在底部 Composer 区域禁用 Codex 原生的 `from-token-main-surface-primary` 渐变背景。
- 覆盖尺寸为 768×130 的主渐变层与 710×28 的辅助渐变层；不修改侧栏、顶栏、右侧浮层或输入交互。

## 本地验证

- Renderer Runtime 回归验证渐变覆盖规则、Composer 一像素 Border 与 `backdrop-filter: none` 同时存在。
- 真实 Codex 任务页计算样式验证：两个原生渐变元素的 `background-image` 均为 `none`。
- 真实窗口截图与像素采样验证：Composer 左右及底部恢复为 `#100D13`，Composer 内部保持 `#1A151F`。
- 短时 Inspector 验证结束后端口 9229 无监听。
- Release 构建 0 警告、0 错误；.NET 测试 137 项通过、2 项需显式授权的现场持久化测试跳过；Node 测试 25 项通过。
- 便携目录共 693 个文件、365,917,779 字节，内置 Node.js 为 v24.18.0。
- ZIP SHA-256：`02639C23C14D50D9DDE2C9F76D23C66D181B067A75FACDB31768D14D9B9E9D15`。

## 安全边界

- 本次不修改 OkkSkin 的 Run 值、Agent、`state.json`、主题文件或持久化配置。
- 干净 Windows/VM、有效病毒扫描和完整 Codex 重启仍未覆盖。
