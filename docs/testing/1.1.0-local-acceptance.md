# Codex Theme Studio 1.1.0 本地验收记录

日期：2026-07-22

## 结果

本机 Windows x64 便携包通过本轮本地验收。该构建仅保存在本机，未连接 GitHub、未创建公开 Release，且未签名。

## 构建与测试

- .NET SDK：`8.0.423`。
- Node.js：随包固定 `v24.18.0`。
- Release 构建：0 警告、0 错误。
- .NET 测试：133 项通过；2 项需要显式授权的现场持久化 enable/disable 测试按设计跳过。
- Injector：24 项 Node 测试通过；Agent 与 Injector self-test 通过。
- `dotnet format --verify-no-changes` 通过。
- NuGet 直接及传递依赖漏洞扫描：未发现当前源已知漏洞。

## 包信息

- 目录：`artifacts/release/1.1.0/CodexThemeManager-1.1.0-win-x64-portable`
- ZIP：`CodexThemeManager-1.1.0-win-x64-portable.zip`
- 文件数：693。
- 解压后字节数：365,907,437。
- ZIP 字节数：151,411,558。
- ZIP SHA-256：`2387ac227f8bad4e691675f83415f9eb73da6683a4a4a2875103402d406e4f57`。
- `CodexThemeManager.exe` SHA-256：`878752fb3f01ca59a06260fc6fb81e753c284641be74fe73745c5a1fdc526481`。
- `CodexThemeStudio.Agent.exe` SHA-256：`0be1a09fe8bf22458489ceb64f2b28134c0a47dabc89302b16923f1939257380`。

上述数量、大小和哈希已独立复算并与 `release-manifest.json`、`SHA256SUMS.txt` 一致。包中未发现 PDB、数据库、日志、测试路径、OkkSkin 状态 JSON、开发机个人绝对路径或用户 Doro 图片。

## UI、RGBA 与 Doro

- 固定深色界面在 96/120/144/192 DPI 的测试渲染中通过；真实 WPF 窗口在 120 DPI 下完成目视检查。
- 六个颜色字段的 Popup 打开、Hex/Alpha 同步、应用和外部关闭回滚通过自动化测试。
- `#RRGGBBAA` 使用 CSS 通道顺序；Doro Border 已从 `rgba(185,138,233,.20)` 规范化为 `#B98AE933`。
- 本机 Doro 以只读 `RemoteSnapshot` 导入，重复启动后仍只有一个 item；实际窗口显示六色色板、“OkkSkin 持久”和“当前外部生效”。
- 在含中文和空格的解压路径中启动成功，主窗口标题正确，随包 Node 为 `v24.18.0`。
- 启动前后 OkkSkin `state.json`、`theme.json`、Run 值、Agent PID 均未变化；结束时 9229 无监听。

## 未覆盖与限制

- 未执行需要改变真实持久化状态的 Theme Studio enable/disable 现场测试。
- 未覆盖干净 Windows/VM、完整 Codex 重启、真实外置磁盘迁移和有效病毒扫描。
- 构建未签名，不作公开分发通过声明。
